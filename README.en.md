# AuroraBot Documentation

This repository contains the AuroraBot documentation site and its single authoritative RFC. The current documentation
covers the reduced AgentTree experimental core, four-role messages, node-local model/tool choices, delegation, and the
project composition root.

Run `npm install`, then `npm run docs:dev` or `npm run docs:build`.
